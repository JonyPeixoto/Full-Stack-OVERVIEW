ORIENTATED OBJECT IN PHP OR OPP

	define();
	$variable;
	func();

	Person (EXAMPLE OF OBJECT, and OBJECT ARE CLASSES THAT WE CREATE ON THE FILE '.PHP' USING SUBLIME TEXT)
	{
		ATRIBBUTES (LOOK LIKE A VARIABLE WHEN WE DO NOT USE ORIENTATION OBJECT):

		name
		age
		weight

		METHOD (LOOK LIKE A FUNCTION WHEN WE DO NOT USE ORIENTATION OBJECT):


		grow()
		eat()


	}